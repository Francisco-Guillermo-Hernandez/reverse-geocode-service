version: '3.8'

services:
  reverse-geocode-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: reverse-geocode-service
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    restart: unless-stopped
